---   
spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
  profiles:
    active: dev
    
    
    
    
---
spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
  profiles:
    active: pro
    
    
